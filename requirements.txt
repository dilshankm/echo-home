# FastAPI
fastapi==0.109.0
uvicorn==0.27.0  # Removed [standard] extras - not needed
pydantic==2.5.3
python-dotenv==1.0.0
# python-multipart==0.0.6  # Only needed for file uploads (not used)

# LangGraph - Multi-agent orchestration
langgraph>=0.0.8

# Graph & Vector
networkx==3.2.1
faiss-cpu==1.7.4
# Removed sentence-transformers and PyTorch - using OpenAI embeddings instead!

# Data Processing
numpy==1.26.3
# pandas==2.1.4  # Only needed if loading from CSV files (optional)

# Optional - only if connecting to real Neo4j Aurora
# Uncomment when ready to connect to Neo4j:
# neo4j==5.15.0

# Utilities (removed - not used)
# requests==2.31.0  # Not used in code
# aiohttp==3.9.1    # Not used in code

# OpenAI (for direct API calls)
openai==1.10.0

